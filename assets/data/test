const [obsecureText, setObsecureText] = useState(false);
<TouchableOpacity
                      onPress={() => {
                        !obsecureText;
                      }}
                    >